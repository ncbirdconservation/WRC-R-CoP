# R comes with a few datasets pre-loaded. Below are some examples.

# display a list of all available default datasets
data()

# displays the first 6 rows of the mtcars dataset
# Motor Trend Car Road Tests
head(mtcars)

# displays the first 6 rows of the iris dataset
# Edgar Anderson's Iris Data
head(iris)

# displays the first 6 rows of the precip dataset
# Annual Precipitation in US Cities
head(precip)
